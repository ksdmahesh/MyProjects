works to do..
--------------

26-Dec-2018
-------------
need to add database Token(Session), Talk(Comment/Ask), Block User(Report), Revision(to go previous), redirected page from(to redirect mainpage from current page).
